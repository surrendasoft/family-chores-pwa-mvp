import { describe, expect, it } from "vitest";import { dashboardSections, getMonday, taskMatchesWeekDay } from "../../src/domain/rules";import type { TaskInstance } from "../../src/domain/types";const weekStart=new Date(2026,5,16);const base:TaskInstance={id:"oneoff",type:"one_off",name:"Move boxes",icon:"📌",frequency:"One-off",assignedTo:"anyone",dueDate:"2026-06-18",status:"available",description:"Move boxes\nClear hallway",createdBy:"laurence",weekStart:"2026-06-16",createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()};describe("AC-One-off",()=>{it("places a one-off task on the exact selected date",()=>{expect(taskMatchesWeekDay(base,weekStart,2)).toBe(true);expect(taskMatchesWeekDay(base,weekStart,1)).toBe(false)});it("shows unclaimed one-off tasks as available",()=>{const sections=dashboardSections([base],"tom",getMonday(weekStart));expect(sections.available.map(t=>t.id)).toEqual(["oneoff"])});it("shows claimed one-off tasks in assignee dashboard",()=>{const claimed={...base,assignedTo:"tom" as const,status:"pending" as const};const sections=dashboardSections([claimed],"tom",getMonday(weekStart));expect(sections.thisWeek.map(t=>t.id)).toEqual(["oneoff"])})});
