import { useMemo, useState } from "react";import type { ActivityEntry, Member, MemberId } from "../domain/types";
export function Activity({activities,members}:{activities:ActivityEntry[];members:Record<MemberId,Member|undefined>}){const[weeksLoaded,setWeeksLoaded]=useState(1);const grouped=useMemo(()=>{const map=new Map<string,ActivityEntry[]>();activities.forEach(a=>{const group=map.get(a.weekStart)||[];group.push(a);map.set(a.weekStart,group)});return Array.from(map.entries()).sort(([a],[b])=>b.localeCompare(a))},[activities]);const visible=grouped.slice(0,weeksLoaded);const canLoadOlder=weeksLoaded<grouped.length;return <section className="screen-panel"><div className="note">Activity shows what happened, grouped by week. It is not chat and not another roster.</div><div className="row section-title"><span>Weekly activity</span><span className="count-pill">{visible.length}</span></div>{visible.map(([week,entries])=><div key={week}><div className="section-title">{week}</div>{entries.map(entry=><div className="activity-item" key={entry.id}><div className={`icon ${entry.type==="done"?"g":entry.type==="swap"?"p":""}`}>{entryIcon(entry.type)}</div><div><strong>{entry.text}</strong><div className="activity-time">{members[entry.actorId]?.name||entry.actorId} · {new Date(entry.createdAt).toLocaleString("en-AU")}</div></div></div>)}</div>)}<button className="ghost full" disabled={!canLoadOlder} onClick={()=>setWeeksLoaded(n=>n+1)}>{canLoadOlder?(weeksLoaded===1?"Load last week":"Load older"):"No older activity"}</button></section>}
function entryIcon(type:ActivityEntry["type"]){if(type==="done"||type==="claimed")return"✓";if(type==="created")return"📌";if(type==="swap")return"↔";if(type==="issue")return"!";return"i"}
