import { useState } from "react";import type { Member, MemberId } from "../domain/types";import { toISODate } from "../domain/rules";import { createOneOffTask } from "../services/firestore";
export function OneOffTaskModal({isOpen,currentMember,members,onClose}:{isOpen:boolean;currentMember:MemberId;members:Member[];onClose:()=>void}){const[name,setName]=useState("");const[assignedTo,setAssignedTo]=useState<MemberId>("anyone");const[dueDate,setDueDate]=useState(toISODate(new Date()));const[dueTime,setDueTime]=useState("");const[description,setDescription]=useState("");const[saving,setSaving]=useState(false);if(!isOpen)return null;async function submit(){if(!name.trim()||!description.trim()){alert("Task name and description / checklist are required.");return}setSaving(true);try{await createOneOffTask({name:name.trim(),assignedTo,dueDate,dueTime,description:description.trim(),createdBy:currentMember});setName("");setAssignedTo("anyone");setDueDate(toISODate(new Date()));setDueTime("");setDescription("");onClose()}finally{setSaving(false)}}return <div className="modal-backdrop show"><div className="modal"><div className="modal-head"><div><h2>Create one-off task</h2><p>Any member can create a single task with one due date.</p></div><button onClick={onClose}>×</button></div><label>Task name</label><input value={name} onChange={e=>setName(e.target.value)} placeholder="e.g. Move boxes from hallway"/><label>Assign to</label><select value={assignedTo} onChange={e=>setAssignedTo(e.target.value as MemberId)}><option value="anyone">Anyone can take it</option>{members.map(m=><option key={m.id} value={m.id}>{m.name}</option>)}</select><label>Due date</label><input type="date" min={toISODate(new Date())} value={dueDate} onChange={e=>setDueDate(e.target.value)}/><label>Due time optional</label><input type="time" value={dueTime} onChange={e=>setDueTime(e.target.value)}/><label>Description / checklist</label><textarea value={description} onChange={e=>setDescription(e.target.value)} placeholder={"Write plain notes, or one checklist point per line:\nMove boxes into garage\nClear hallway\nStack safely"}/><button className="primary full" disabled={saving} onClick={submit}>{saving?"Creating...":"Create task"}</button></div></div>}
