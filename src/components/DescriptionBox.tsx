import { descriptionLines } from "../domain/rules";
export function DescriptionBox({text,compact=false}:{text:string;compact?:boolean}){const lines=descriptionLines(text);const shown=compact?lines.slice(0,3):lines;if(!shown.length)return <div className="description-box"><div className="desc-label">Description / checklist</div><div className="desc-text">No description added.</div></div>;return <div className="description-box"><div className="desc-label">Description / checklist</div><ul>{shown.map(line=><li key={line}>{line}</li>)}{compact&&lines.length>3&&<li>+{lines.length-3} more</li>}</ul></div>}
