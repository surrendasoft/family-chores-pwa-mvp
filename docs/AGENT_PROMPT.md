# Cursor Agent Prompt

You are taking over a working React + Firebase PWA MVP.

Source of truth:

1. `docs/RDD.md`
2. `tests/acceptance`
3. Existing React implementation
4. `docs/CURRENT_MOCKUP.html` visual reference

Do not add out-of-scope features.

Run:

```bash
npm install
npm test
npm run build
npm run dev
```

Definition of done:

- Tests pass
- Build passes
- Firebase data persists
- One-off task creation/claiming/completion works
- Rotation assignment works
- Dashboard/Week/Activity/Profile work on mobile viewport
